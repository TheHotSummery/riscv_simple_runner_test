# 复制为 .env 或直接在 shell 中 export（python-dotenv 会在启动时加载）

# ── 必填 ─────────────────────────────────────────────────────────────
GITHUB_TOKEN=
TARGET_BRANCH=main

# ── 单仓库（默认 WORKSPACE_MODE=git）────────────────────────────────
GITHUB_REPO=owner/repo

# ── 多仓库 repo manifest（WORKSPACE_MODE=repo）───────────────────────
# WORKSPACE_MODE=repo
# MANIFEST_REPO=org/manifest
# MANIFEST_BRANCH=main
# MANIFEST_FILE=default.xml
# MANIFEST_GITHUB_ORG=spacemit-robotics
# WATCH_REPOS=spacemit-robotics/build,spacemit-robotics/kernel
# REPO_BOOTSTRAP_NON_INTERACTIVE=1   # systemd 等无 TTY：init/sync 失败只打印诊断并退出，不交互选菜单
# REPO_INIT_NO_CLONE_BUNDLE=0        # 仅当 repo init 不支持 --no-clone-bundle 时设为 0
# REPO_REPO_URL=https://mirrors.tuna.tsinghua.edu.cn/git/git-repo   # init 时克隆 git-repo 的地址（默认 gerrit 国内常不可用）

# ── 可选 ─────────────────────────────────────────────────────────────
# WORKSPACE_DIR=./workspace
# WORKFLOW_DIR=./workspace/build   # 统一构建入口（最高优先级，留空=自动查找）
# POLL_INTERVAL=15
# STEP_TIMEOUT=3600
# SKIP_SUDO_STEPS=true
# MAX_PARALLEL_JOBS=1
# LOG_DIR=./logs
# RUNNER_BOARD=deb1
# POST_PR_COMMENT=true
# ALLOWED_AUTHORS=alice,bob
